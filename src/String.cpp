#include <String.h>
#include <stdio.h>
#include <stdlib.h>

/*
String::String()
	: buffer(NULL)
{
}

String::String(const char const c)
{
	buffer = Create(c);
}

String::String(const char* const str)
{
	buffer = Create(str);
}

String::~String()
{
	Clear();
}


char* String::Create(const char* const);
{
}

void String::Clear()
{
	if (buffer != NULL)
	{
		delete[] buffer;
		buffer = NULL;
	}
}
*/
